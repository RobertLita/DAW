import { HoverPozaDirective } from './hover-poza.directive';

describe('HoverPozaDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPozaDirective();
    expect(directive).toBeTruthy();
  });
});
